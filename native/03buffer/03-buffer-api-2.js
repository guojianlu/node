// concat
/* let b1 = Buffer.from('前端')
let b2 = Buffer.from('开发')

let b = Buffer.concat([b1, b2])
console.log(b)
console.log(b.toString()) */

// isBuffer
/* let b1 = Buffer.alloc(3)
let b2 = '123'
console.log(Buffer.isBuffer(b1))
console.log(Buffer.isBuffer(b2)) */